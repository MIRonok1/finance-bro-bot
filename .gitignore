.env
*.db
*.db-journal
*.db-wal
*.db-shm
local.db
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# Frontend (webapp/)
webapp/node_modules/
webapp/.vite/
webapp/*.tsbuildinfo

# webapp/dist НЕ игнорируем (сознательное исключение, см. README/CLAUDE.md):
# Netrun деплоит через автодетект Python и не собирает Dockerfile-стадии,
# поэтому собранный фронтенд коммитим как есть. Пересобирать и коммитить
# заново при каждом изменении webapp/src.
